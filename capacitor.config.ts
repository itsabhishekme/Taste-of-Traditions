import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tasteoftraditions.app",
  appName: "Taste of Traditions",
  webDir: "out",
};

export default config;